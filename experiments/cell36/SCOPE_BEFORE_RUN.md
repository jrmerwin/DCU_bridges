# Cell 36 — fixed scope before the interaction calculations

Question: can the already found balanced four-path attachment preserve its three
coherent phase words while a specified interaction reads or changes their label?
Keep the native DCU counting law and Cell35 storage attachment unchanged.

Inputs: all register- and entry-balanced example witnesses already saved in the
Cell35 completed collective panels, all seven epoch checkpoints, both H regimes,
and their actual parent cards. These are archived representatives, not an
exhaustive new census. The detailed finite native transition audit uses the first
reported entry-balanced assembly at H0, seed20350920, tau128, with its full actual
19-object checkpoint. No further random history is required. The later same-world
checkpoint is retained as an age/control input, not regenerated.

1. Validate actual paths and register/entry multiplicities. Verify exact scalar
   action on C=span{|00>,|11>,|22>} for every register of each saved example under
   the unchanged primary driver. Retain failures of the secondary entry-sensitive
   driver; do not replace primary by secondary. Use Q=27 and81, as before.
2. Enumerate all legal next maintenance subsets at the detailed checkpoint,
   with multiplicity binomial(F,3-|W|). Construct every absent pair and compute
   original first/repeat work. Show the classical successor kernel is identical
   for all three code words. This tests growth/work response, not all instrument
   outcomes. The full source expressly leaves record values out of counting.
3. Test the existing zero-setting local Fourier readout: correct label and the
   conditional memory state, using the ordinary projective state-update hypothesis.
   Its POVM probabilities are supplied; repeatable physical implementation is not.
4. Apply the existing phase operation D(1/3) on one factor as a logical label
   transition, and reapply every storage generator. It is an ideal commanded
   operation, not an autonomous native event.
5. Separately define a feasibility model for a nondestructive interface: a new
   probe qutrit, Fourier preparation/readout, and a probe-controlled COMMON cyclic
   shift X^m tensor X^m on the memory. This controlled coupling is NEW, not claimed
   native/forced by the constructor. Test full unitary, all three words, repeated
   label readout, and a logical superposition with probe retained vs discarded.
6. Resolve the common shift into two probe-controlled single-factor shifts. Insert
   one allowed storage service burst between them, retaining the native exact
   weights. Also report results conditional on exactly one assembly-register hit,
   and enumerate all Q phase residues as a gate sensitivity audit. Do not optimize
   duration, phase depth, or input state. A service gap is not a physical pulse
   duration; external control and resource cost remain uncompiled.

No added state-dependent sampling, energy assignment, temperature, SI unit, mass
coefficient, observer identity, neutron predicate, or private search input. A
positive ideal circuit is not a native mass or neutron result. A negative native
counting response at fixed structure does not exclude different structural states.
