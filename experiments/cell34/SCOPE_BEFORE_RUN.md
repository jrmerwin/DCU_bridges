# Cell 34 — bounded architecture/protection test

Primary calculation: all unordered pairs in the FOUR fixed Cell26 panels (record
lengths 3 and 4; two native checkpoints). Reuse the exact Cell26 +/- support-count
coupling to its attached two-qutrit apparatus, at both existing phase moduli 27/81.
Do not assign quantum tensor factors to record paths themselves. Compute common
character eigenspaces exactly using integer phase signatures. At these initial
states an identity draw and a singleton draw for each register are legal, so their
signatures characterize exact all-history protection, not a sampled eigenvalue test.

Readability: encode three phase words in the source's diagonal shared-source code
using the declared local D(k/3) operations; decode by the existing zero-setting
local Fourier measurements and outcome-difference comparison. No storage pulses,
feedback, history selection, new native workload or physical calibration. Test
coherence explicitly, not merely preservation of computational populations.

Use every saved Cell26 history and checkpoint. Numerical histories illustrate the
already fixed coupling; they are not independent physical validation. Reproduce
all prior overlap-class readout means. No new large simulation is planned.

Negative controls: unique-support quotient; separate-channel/disjoint-support
cases; full computational dephasing of the protected phase words. A separately
labeled sensitivity model conjugates each register phase generator by the existing
entry reflection S=(0 1); it is NOT the primary model, a derived physical coupling,
or a replacement chosen after seeing the result. Classify it under the same rule.
Retain entry-oriented paths as distinct, and demonstrate that reflection is not
silently a +/- sign. No new mass/proton/neutron identification.

Stop criterion for this bounded test: if all two-factor protection reduces to
identical driving supports, report that exact result and do not turn it into a
matter claim or launch a search for rare pair-level exceptions forbidden by it.
