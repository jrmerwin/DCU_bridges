# Cell 32 — source-guided smaller vertical test

Pause the continuing-observer/identity branch. Do not patch the neutron search.

Use every saved Cell29 history and checkpoint, six inherited arms, no filtering.
No fresh simulation is needed for the primary result. Counting conventions, carrier
IDs 3/4, sum phase increment 1/27 and 1/81 control stay unchanged.

Preparation: the paper's declared |Omega_2> shared-source qutrit state. Spectator
phase retention remains the Cell25/29 hypothesis. Native history drives local
D(N3/q) tensor D(N4/q); q here is phase denominator, NOT capacity.

Primary measurement: fixed signed CGLMP3 with settings (x0,x1,y0,y1)=
(0,4/27,-2/27,2/27). These are nearest denominator-27 approximations to the paper's
continuous maximizing settings, with the x1 tie rounded toward zero. The SAME
measurement quartet is used for both phase increments. No optimization or
history-dependent measurement selection in the primary comparison.

Compute density matrix, two phase moments, all 36 joint probabilities, fixed Bell
score and partial-transpose negativity. Keep late-time negative witness values;
absence of violation of this witness is NOT a separability claim.

Controls: direct tensor calculation; source continuous benchmark as a regression
only; every deterministic local outcome assignment to check Bell bound; complete
phase erasure; mean-phase-only pure surrogate; inverse phase conditioned on local
service counters without dropping histories. The latter is a formal instrument
operation, not a compiled/costed observer feedback implementation.

Derive all-state channel from JOINT (N3,N4) histogram. The K=N3+N4 histogram is
sufficient only for the specified diagonal shared-source state, not arbitrary
joint quantum states.

Already-known exploratory information: a short arithmetic diagnostic using the
saved data preceded this specification. This is a retrospective saved-history
analysis, not a blind or preregistered physical test. No mechanism or data selection
was changed based on its numerical results.

## Source-backed transfer added after the first direct-source calculation

The paper's legal P01 F3 P01 heralded state supplies a second, non-diagonal source.
Its operation, success 4/9 and Schmidt spectrum (3/4,1/4,0) are fixed by the source.
Apply it to Alice BEFORE all unchanged clock histories. Reuse the exact joint
local-phase channel; do not choose a new state to improve robustness. This transfer
was explored after the initial direct-source calculation; no blind status is claimed.
Keep the invalid total-count-only reduction as an explicit negative control: that
reduction is correct for Omega_2 but not for this general source. The filter is an
attached instrument operation, not a newly compiled/counting-charged gate.
