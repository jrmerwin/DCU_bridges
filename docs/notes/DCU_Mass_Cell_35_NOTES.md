# Cell 35 — later native epochs, local-activity controls, and collective balance

## Summary

The original two-path phase-memory test has now been carried to genuine
maintenance ages up to4,194,304, on eight native histories. Later preparation gives
much higher decoding success over a fixed global-tick window. The same early
support pair, tested again at the late epoch in fresh continuations, does NOT gain
readout protection when the number of services to its actual registers is fixed.

A separately labeled four-input extension finds readable protected combinations
of four DISTINCT native paths, including entry-token-balanced examples. These
already occur at128 ticks in some histories and at1024 in all eight. They are
collective shared-drive cancellations, not a particle, energy gap, or evidence
that the two-path theorem was wrong.

Nothing here establishes a necessary-and-sufficient criterion for matter. The
native source and particle dictionary remain distinct. No calibrated proton or
neutron epoch is known from this assay.

## Source and frozen conventions

The supplied September2026 revised0.5 DCU manuscript, Sections2.1–2.5, supplies
unordered absent pairs, ancestry closure, type-blind service, complete bursts,
first/repeat recording charges, relief, and tau=sum(served maintenance). Section5.1
supplies primitive-rooted recorded-endpoint paths and entry tokens. Section7.1
supplies the conditional complex instrument and finite phase settings. The file
is `unified_deu_eq (1)(1).pdf` in the conversation.

Cell26 supplies the added support-count fan-out. Cell34 supplies its readable
three-word code and exact two-path classification. Neither paths nor arbitrary
motifs are newly identified as physical quantum tensor factors. The original
source's record values remain passive: they do not enter the counting transition.

The older *Distinction Engine* paper's typed condensation simulator has explicit
S/G/I state spaces, energy, and state-changing rules. It is NOT the dynamics run
here. Its condensation claim is not being contradicted or reproduced by this
phase-storage test. The RMR mass/screening equations supply no selected parameters
in this experiment.

## 1. Native simulation, ages and resource caps

Gamma=3, m=0, H in{0,6}, first-use coefficient11, repeat2. Four genesis seeds per
regime,20350920–20350923. The initial two deterministic updates prepare the normal
five-object state with26 outstanding requests and tau5. Nothing is cleared.

The full process is advanced to tau4,456,448, with observation targets
128,1024,8192,65536,524288,1048576,4194304. Observations occur AFTER the whole
service and birth burst first crossing the target. A possible overshoot0..2 is
retained. Iterations, tau, n,F,P, birth and first-write counters remain separate.
The native run is bounded by4,000,000,000 chronological updates and50,000 objects.
All eight worlds reached every target. The final primary age is4,456,448 each;
summing their ticks does not make one older world.

The primary simulation executed4,391,091,211 complete chronological updates.
Only updates with nonempty maintenance are recorded in the large raw event file;
all-force updates are still actually executed. No mean clock, productive-only
clock, queue jump or asymptotic inversion is substituted. Omitted log rows are
independently checked as deterministic all-force ledger updates between events.

An integer SplitMix64 stream with unbiased rejection for bounded integers replaces
Python's RNG stream, NOT its sampling probability. Each service is an ordered
uniform without-replacement sample from all F+n tokens, then projected to the
maintenance subset. The compiled port is checked against the original Python
constructor and the original Cell25 step with identical explicit token draws.
Integer overflow guards fail rather than approximate charges or chain counts.

The high-age extension to4,194,304 was chosen using an implementation-only timing
benchmark before any protection output was inspected. See SCOPE_BEFORE_RUN.md.

## 2. Reselection rather than reusing only early structures

At EACH epoch, enumerate every currently recorded-endpoint path with exactly3 or4
composite registers. These are the fixed-length primary panels. Retain all entry
information; do not use future recorded status. Within every(length, symmetric-
difference size) stratum, select up to16 unordered pairs uniformly with an
independent deterministic selector. Save the complete eligible count.

A coverage control selects up to64 endpoints first written between tau/8 and the
current epoch, then draws two UNIFORM complete primitive-rooted paths per endpoint.
This uses exact integer chain-path counts. Duplicate exact paths are counted and
then deduplicated; equal-length pairs are sampled up to4 per stratum. These paths
reach lengths5–27 in the last epoch, unlike the fixed three/four-token panel.
This is a sampled coverage control, not an exhaustive search of all architectures.

There are176,491 path appearances across epochs/cohorts,340,303,567 eligible
pair appearances, and16,607 selected paired readouts. Repeated paths, overlaps and
epochs in one native history are correlated. These are NOT independent physical
observations. The aggregate gives equal weight to each world, not every pair.

The selected supports are frozen during each subsequent observation. The
surrounding native universe continues growing. Memory preparation and terminal
readout are ideal, counterfactual measurements, not costed native operations.
There are no intermediate projective measurements or state resets during storage.

## 3. Measurements and censoring

The unchanged primary law is

    U = D(N_A/27) tensor D(-N_B/27),
    D(x)=diag(1,exp(2pi i x),exp(4pi i x)).

The prior modulus81 control is retained. The original three codewords are
Omega_k=sum_r exp(2pi i k r/3)|rr>/sqrt(3), decoded by the joint Fourier outcome
b-a modulo3. Correct-word probability on one history is

    Pcorrect=[1+2cos(2pi(N_A-N_B)/Q)]^2/9.

Primary global windows are128,512,2048,8192 additional MAINTENANCE ticks.
Local windows are8,32,128 services to A union B, counting a shared register once.
Each read is at the first complete burst crossing the threshold; all overshoots
and incomplete milestones remain saved. A shared register contributes to both
path clocks but only once to union activity.

At the final epoch the delta2 length3 panel does not complete128 union services
within the primary262,144-tick follow-up. ALL128 such cases across both regimes
are retained as censored. No complete-case success is claimed for that comparison.
The saved summaries include [lower,upper] bounds allowing every missing terminal
probability to range over[0,1]. The32-service comparison is completed throughout.

## 4. A cleaner longitudinal confirmation

After inspecting the first pilot world's pattern, add a separately frozen
confirmation: in EACH of eight worlds select the first lexicographic length3 pair
with symmetric difference2 at tau128. Carry exactly those supports to tau4,194,304.
Run32 fresh continuations from each of these16 checkpoints,512 in total. Every
continuation measures global8192 and local32. All complete. Same numbered fork
seeds across early/late are paired starts, not identical ensuing service draws.

This preserves architecture within each world and changes its native environment.
It is not the fresh-at-every-epoch selection from the primary panel, and does not
replace that panel. It adds4,682,046,623 native iterations. All source-state input
files and all resulting counts are included in replication/.

Primary Q27 correct-codeword results (conditional Monte Carlo SE, four fixed
source worlds with32 fresh continuations each):

|Regime|Age|After8192 global ticks|After32 union-register services|
|---|---:|---:|---:|
|H0|128|0.310502 ±0.029529|0.668240 ±0.026569|
|H0|4194304|0.958638 ±0.004983|0.642138 ±0.028366|
|H6|128|0.342947 ±0.030998|0.681919 ±0.027193|
|H6|4194304|0.968037 ±0.004933|0.653018 ±0.027298|

The SE is sqrt(sum(within-world variance/32))/4. It conditions on this four-world
starting panel. Between-world spread and paired late-minus-early errors are also
retained. Do not treat all overlapping primary path pairs as independent trials,
and do not interpret these errors as laboratory uncertainties.

During8192 global ticks, mean union services fall from329.24 to2.77 for H0, and
from215.47 to2.01 for H6. To accumulate32 actual union services takes183.38 versus
95,238.46 additional global ticks for H0, and313.55 versus137,324.01 for H6 in
these finite continuations. These are observed finite-sample durations, not proofs
of an asymptotic mean waiting-time law or physical clock calibration.

## 5. Exact activity-normalized law

Let u=|A union B|, delta=|A symmetric_difference B| for equal-size supports.
Condition on exactly j union-register requests being served in an update.
Their identities are a uniform j-subset of the union, irrespective of epoch or
queue. The coefficients are+1 on A\B, -1 on B\A, and0 on the intersection.
They sum to zero and have population second moment delta/u. Hence

    E[dD | current state,j]=0,
    Var(dD | current state,j)=j*(delta/u)*(u-j)/(u-1).

For the matched length3,shared2 pair, u4,delta2. Singleton union hits have signs
+1,-1,0 with probabilities1/4,1/4,1/2, and variance1/2. The global workload alters
when those hits occur, not their conditional mark distribution. Multiple hits
supply the explicit without-replacement factor; the actual simulation retains
them. No iid or Poisson approximation is used in the reported native predictions.

The measured D^2 after32 union services is15.22/15.91 (early/late,H0) and13.67/16.27
(H6), around the singleton-dominated reference16. Full finite-state dependencies
are not replaced by this limiting reference. The exact two-path protected-space
classification itself still has no age variable when singleton services are legal.

## 6. Separately named four-input structural extension

Within each length panel, keep a canonical path representative for each DISTINCT
register support. Enumerate equal sums of support vectors:

    1_A+1_B = 1_C+1_D,

with A,B,C,D all different. Also check equality of the multisets of(z,entry) tokens.
The exhaustive four-input census is capped at2048 distinct supports per panel;
above that it is explicitly uncomputed, NOT assigned zero candidates. This affects
the length4 collective census at the three latest epochs, not primary path-pair
sampling or phase measurements. Do not compare totals across that coverage change
as evidence of declining structure counts.

The added attachment drives the ORIGINAL TWO-qutrit apparatus by

    D((N_A+N_B)/Q) tensor D(-(N_C+N_D)/Q).

No four-sibling source or Gamma5 preparation is silently supplied. This is an
extension of the control wiring, not the unchanged two-path test. Both aggregate
inputs receive identical increments, protecting span{|00>,|11>,|22>} and the
original three readable codewords. Token balance also preserves this space under
the previously labeled entry-conjugated variant S^e G S^e, S=(0 1).

Actual example at tau128,H0,seed20350920 (IDs are storage labels):

    A: (4,entry0),(7,entry0),(10,entry1)
    B: (5,entry0),(7,entry1),(11,entry1)
    C: (4,entry0),(7,entry0),(11,entry1)
    D: (5,entry0),(7,entry1),(10,entry1).

All four paths begin at primitive1. Their full supports are different, yet both
register and entry-token multiplicities balance. After8192 global ticks their
counts are219,239,220,238: 219+239=220+238. Equality is pathwise for EVERY service
history, not inferred from this sample. It remains exact while old registers
are retained; no future event outside the support changes this proposed wiring.

Entry-balanced examples occur in6 of8 worlds at128 ticks, and all8 at1024. At
4,194,304 the COMPLETE length3 panels still contain them; the larger length4
collective census was not completed under the fixed cap. The randomly sampled
recent-full-path panels produced no four-path equality; their incompleteness
precludes a global absence claim.

This is not stronger protection arising at a late matter epoch. It is the already
known shared-driving principle realized by a larger path-recombination identity.
It does demonstrate why a two-input impossibility is not an all-architecture
impossibility. It does not generate a costed autonomous memory, energy, mass,
confinement, charge, particle species or spatial dimension.

## 7. Reproducibility and interpretation limits

See TESTS for actually executed checks. The raw log contains every positive-
maintenance service; all quiet intervals are checked against exact ledger updates.
Independent late-burst charges use backward ancestry bitsets and full burst
multiplicity, not the same cost implementation. The original Python sampler step
is replayed using explicit draws. Selection and decoding are checked separately.

Two implementation optimizations preserve the scientific result: contiguous
signed tick arrays avoid copies in binary searches; byte-valued overlap tables
replace millions of Python pair tuples while returning IDENTICAL selected pairs
and counts under the same uniform selector. No epoch, state, coefficient or
measurement target was changed because of a numerical readout.

Four million maintenance ticks is not four million iterations, and neither is
assigned seconds. The Mac neutron scanner may use different parameters/counters;
these runs are not claimed to reproduce its physical maturity target. The present
result restricts a PARTICULAR native-to-quantum attachment, not all matter in
DCU and not the conditional numerical RMR mass dictionary.
